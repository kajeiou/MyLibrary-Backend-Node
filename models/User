class User {

    constructor( userName, email, password, roleId) {
        this.userName = userName
        this.email = email
        this.password = password
        this.roleId = roleId
    }
}
module.exports = {User}